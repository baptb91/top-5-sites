
# Netlify redirects file
# Nous protégeons le fichier XML des redirections SPA
/sitemap.xml /sitemap.xml 200
/sitemap-index.xml /sitemap-index.xml 200
/robots.txt /robots.txt 200

# Ensuite, on gère toutes les autres routes avec le SPA router
/* /index.html 200
