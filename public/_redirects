# Redirections Netlify - SPA React pour tous
# Les fichiers HTML statiques sont uniquement pour les bots via netlify.toml

# Redirection du sitemap principal
/sitemap.xml /sitemap-v2-index.xml 301
/sitemaps.xml /sitemap-v2-index.xml 301

# Fallback SPA pour tous les utilisateurs (React Router gère les routes)
/* /index.html 200
